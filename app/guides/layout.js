// Title lives here because page.js is a client component (scrollspy).
export const metadata = {
  title: "Guides · The Travel Nurse Guide",
  description:
    "Straight-talk guides on travel nurse pay, contracts, taxes, and housing. Written by a working RN, not a recruiter.",
};

export default function GuidesLayout({ children }) {
  return children;
}
