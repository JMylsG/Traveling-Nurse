// Title lives here because page.js is a client component (scrollspy).
export const metadata = { title: "Guides · The Travel Nurse Guide" };

export default function GuidesLayout({ children }) {
  return children;
}
