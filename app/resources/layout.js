// Title lives here because page.js is a client component (scrollspy).
export const metadata = {
  title: "Resources · The Travel Nurse Guide",
  description:
    "A vetted short list of tax help, insurance, certifications, and housing for travel nurses. Recommendations, not ads.",
};

export default function ResourcesLayout({ children }) {
  return children;
}
